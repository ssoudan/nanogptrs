//! # The `nanogptrs` crate

/// The `data` module contains the structs and functions for loading, tokenizing the data and
/// generating batches.
pub mod data;

/// The `learn` module contains the structs and functions for training and evaluating the model.
pub mod learn;

/// The `model` module contains the structs and functions for the model.
pub mod model;
